/*
Pas de variables globales,
Pas d'allocation de mémoire (non vu en cours),
Pas de code inutilement complexe (non vu en cours).
Le fichier main.c ne doit être modifié par personne,
Les fichiers non intégrés au jeu ne doivent pas être créés dans le dossier `src` mais plutôt `lib/fonctions/`
Pas de fichiers .h pour l'instant (non vu en cours),
Les fonctions qui concernent une même structure ou entité doivent être groupés dans le même fichier.
*/

#include <nom_de_la_librairie>

#define MA_CONSTANTE VALEUR

// Prototypes des fonctions
type maSuperFonction(type argument1, type argument2);

// Déclaration des fonctions
type maSuperFonction(type argument1, type argument2)
{
    //
}